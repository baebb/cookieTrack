$(document).ready(function () {

    $('.but1').click(
        function () {
            //first event
        }
    );
    $('.but2').click(
        function () {
            //second event
        }
    );
});